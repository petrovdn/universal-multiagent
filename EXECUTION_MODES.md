# Режимы выполнения задач

## Обзор

Система поддерживает 4 режима выполнения задач, каждый из которых использует **UnifiedReActEngine** под капотом, но с разными настройками и поведением.

## Архитектура

Все режимы используют единую архитектуру **ReAct (Reasoning + Acting)**:
- **Query Mode** → `QueryModeAdapter` → `UnifiedReActEngine` (только READ)
- **Agent Mode** → `AgentModeAdapter` → `UnifiedReActEngine` (READ + WRITE)
- **Plan Mode** → `PlanModeAdapter` → `UnifiedReActEngine` (READ + WRITE, с планированием)
- **ReAct Mode** → `ReActOrchestrator` (legacy, прямой ReAct)

## Режим 1: Query (Вопрос) 🔍

**Назначение:** Только чтение данных, без модификаций.

**Использует:** `QueryModeAdapter` → `UnifiedReActEngine` с конфигурацией:
- `allowed_categories: [READ]` - только чтение
- `show_plan_to_user: False` - план не показывается
- `require_plan_approval: False` - подтверждение не требуется
- `enable_alternatives: True` - альтернативные пути разрешены

**Особенности:**
- ✅ Все действия скрыты от пользователя (только результат)
- ✅ Reasoning блоки скрыты по умолчанию (можно включить в настройках)
- ✅ Промежуточные шаги не видны
- ✅ Финальный результат в человеческом виде
- ✅ Идеально для: чтения файлов, поиска данных, генерации отчетов

**Когда использовать:**
- "Посмотри встречи на этой неделе"
- "Найди письма от alice@example.com"
- "Покажи данные из 1С за январь"

---

## Режим 2: Agent (Агент) 🤖

**Назначение:** Автономное выполнение с полным доступом к возможностям.

**Использует:** `AgentModeAdapter` → `UnifiedReActEngine` с конфигурацией:
- `allowed_categories: [READ, WRITE]` - чтение и запись
- `show_plan_to_user: False` - план не показывается
- `require_plan_approval: False` - подтверждение не требуется
- `enable_alternatives: True` - альтернативные пути разрешены

**Особенности:**
- ✅ Все действия видны в реальном времени (ActionItem компоненты)
- ✅ Reasoning блоки видны (collapsible)
- ✅ Статусы действий обновляются в реальном времени
- ✅ Ошибки обрабатываются с показом альтернатив
- ✅ Идеально для: создания файлов, отправки писем, выполнения задач

**Когда использовать:**
- "Создай документ с отчетом"
- "Отправь письмо на alice@example.com"
- "Объедини file1.txt и file2.txt"

---

## Режим 3: Plan (План) 📋

**Назначение:** Планирование с подтверждением пользователя перед выполнением.

**Использует:** `PlanModeAdapter` → `UnifiedReActEngine` с конфигурацией:
- `allowed_categories: [READ, WRITE]` - чтение и запись
- `show_plan_to_user: True` - план показывается пользователю
- `require_plan_approval: True` - требуется подтверждение
- `enable_alternatives: True` - альтернативные пути разрешены

**Особенности:**
- ✅ Фаза 1: Исследование (read-only)
- ✅ Фаза 2: Планирование (генерация markdown плана)
- ✅ Фаза 3: Подтверждение пользователем (можно редактировать)
- ✅ Фаза 4: Выполнение (BUILD) с прогресс-баром
- ✅ Reasoning блоки видны во время планирования
- ✅ Уточняющие вопросы (QuestionForm) для уточнения деталей
- ✅ Идеально для: сложных многошаговых задач, интеграций, генерации кода

**Когда использовать:**
- "Создай интеграцию с новым CRM"
- "Настрой автоматизацию для отчетов"
- "Сгенерируй код для API"

---

## Режим 4: ReAct (Legacy) 🔄

**Назначение:** Прямой ReAct режим (legacy, для обратной совместимости).

**Использует:** `ReActOrchestrator` (legacy, не UnifiedReActEngine)

**Особенности:**
- ✅ Reasoning блоки всегда видны и развернуты
- ✅ Полный reasoning trail показывается
- ✅ Все итерации ReAct цикла видны
- ⚠️ Legacy режим - рекомендуется использовать Agent Mode

**Когда использовать:**
- Только для обратной совместимости
- Для отладки reasoning процесса

---

## Маппинг режимов

Frontend отправляет режимы, которые маппятся на backend:

| Frontend Mode | Backend Mapping | Adapter |
|---------------|------------------|---------|
| `query` | `query` | `QueryModeAdapter` |
| `agent` | `agent` | `AgentModeAdapter` |
| `plan` | `plan` | `PlanModeAdapter` |
| `react` | `react` | `ReActOrchestrator` (legacy) |
| `instant` | `agent` | `AgentModeAdapter` |
| `approval` | `plan` | `PlanModeAdapter` |

---

## Использование ReAct

**Все режимы (Query, Agent, Plan) используют ReAct под капотом** через `UnifiedReActEngine`:

1. **Query Mode** - ReAct с ограничением только READ операций
2. **Agent Mode** - ReAct с полным доступом (READ + WRITE)
3. **Plan Mode** - ReAct с планированием и подтверждением

**ReAct Mode (legacy)** - прямой ReAct без адаптеров.

---

## Настройки отображения

### Reasoning блоки

- **Query Mode:** Скрыты по умолчанию (можно включить в настройках `showReasoning`)
- **Agent Mode:** Видны (collapsible, auto-collapse)
- **Plan Mode:** Видны во время планирования
- **ReAct Mode:** Всегда видны и развернуты

### Действия (ActionItem)

- **Query Mode:** Скрыты (действия не показываются)
- **Agent Mode:** Видны в реальном времени
- **Plan Mode:** Видны во время выполнения (BUILD)
- **ReAct Mode:** Видны внутри reasoning блоков

---

## Рекомендации по выбору режима

1. **Query** - для чтения данных и получения информации
2. **Agent** - для быстрого выполнения задач без планирования
3. **Plan** - для сложных задач, требующих планирования и подтверждения
4. **ReAct** - только для отладки (legacy)

---

**Версия:** 1.0  
**Дата:** 2024

