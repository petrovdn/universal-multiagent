#!/bin/bash

# Скрипт для установки всех зависимостей проекта

set -e

echo "🚀 Установка зависимостей для Google Workspace Multi-Agent System..."

# Проверка Python
if ! command -v python3 &> /dev/null; then
    echo "❌ Python 3 не найден. Установите Python 3.10+"
    exit 1
fi

echo "✅ Python найден: $(python3 --version)"

# Создание виртуального окружения (опционально, но рекомендуется)
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi

# Активация виртуального окружения
echo "🔌 Активация виртуального окружения..."
source venv/bin/activate

# Обновление pip
echo "⬆️  Обновление pip..."
pip install --upgrade pip --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org

# Установка зависимостей
echo "📚 Установка Python зависимостей..."
pip install --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org -r requirements.txt

# Установка MCP серверов (опционально, для внешнего Gmail MCP)
echo "📦 Установка MCP серверов (опционально)..."
echo "Примечание: Проект использует собственные MCP серверы для Calendar и Sheets"
echo "Gmail MCP можно установить для внешнего использования (опционально):"
echo "  npm install -g @gongrzhe/server-gmail-autoauth-mcp"

echo ""
echo "✅ Установка завершена!"
echo ""
echo "Следующие шаги:"
echo "1. Создайте config/.env с вашими учетными данными"
echo "2. Настройте Google OAuth credentials в config/.env"
echo "3. Запустите: python3 src/main.py"
echo "4. Авторизуйте интеграции через веб-интерфейс приложения"
echo ""
echo "Для активации виртуального окружения в будущем:"
echo "  source venv/bin/activate"



